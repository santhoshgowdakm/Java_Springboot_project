spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hrs_prod
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
server:
  port: 8083
